1. based on api and db schema create types for api response, api request and common types in application
2. based on these types and documentation create mock data for all API routes. Always use types for mocks. Find relevant images on the internet. Don't use other libraries for mock data. 
3. based on these types and documentation create routes for the apllication and fill them with mock data. Always use defined types for routes or if necessary create new types in types folder. Do not create mocks directly in the routes.